(setq *print-case* :capitalize)

;;;; Lambda 匿名函数

(mapcar (lambda (x) (print (* x 2))) '(1 2 3 4 5))
#||
2 
4 
6 
8 
10
||#

#||
(lambda (x) (print (* x 2)))
这部分定义了一个匿名函数，
它接受一个参数x，然后计算x的两倍，
并使用print函数将结果输出到标准输出。
这个匿名函数实际上是一个函数对象，
可以被传递给其他函数，例如mapcar。

(mapcar ... '(1 2 3 4 5))
这是mapcar函数的调用。
mapcar用于对一个或多个序列中的每个元素应用指定的函数，
并返回结果构成的新列表。
在这里，我们传递了匿名函数 (lambda (x) (print (* x 2))) 
作为第一个参数，以及列表 (1 2 3 4 5) 作为第二个参数。

现在，让我们看看代码的执行过程：

mapcar会遍历列表 (1 2 3 4 5) 中的每个元素，依次将它们传递给匿名函数。
匿名函数会计算每个元素的两倍，并使用print函数将结果输出到标准输出。
||#